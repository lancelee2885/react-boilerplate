/*
 * AddCrypto Messages
 *
 * This contains all the text for the AddCrypto container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddCrypto';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddCrypto container!',
  },
});
